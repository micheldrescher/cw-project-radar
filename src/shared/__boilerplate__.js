// simple boilerplate JS function used for the test module
// when coding, remove this and the corresponding boilerplate test class

const adder = (a, b) => {
    return a + b
}

export { adder as default }
